package edu.utc.bkf926.WorldStream;

import java.io.IOException;

public class WSJSONWriter {
	
	public void writeBlock(WSBlock block) throws IOException{
		//TODO Write a single block
	}
	
	public void writeChunk(WSChunk chunk) throws IOException{
		//TODO 
	}
	
	public void writeLoadedChunks() throws IOException{
		//A loop or recursion to call writeChunk() on every loaded chunk
	}
	
	public void writeEntireMap() throws IOException{
		//Another loop to write every chunk in the world. Not sure how intensive this will be.
		//Maybe use this once when the world is written for the first time, then only update one chunk at a time
		//unless specified to write loaded chunks by the player.
	}
	
}
